#!/bin/bash

#take random sleep
#play sound and show notify
DELAY=$(shuf -i 0-2 -n 1).$(shuf -i 0-9 -n 1)
#echo $DELAY
sleep $DELAY
notify-send -h int:x:500 -h int:y:500 -i $ico -u "$1"
#aplay or mpv&
# make conreate signals for actions x, eat, train
